import 'package:flutter/material.dart';

class InfoPg2 extends StatelessWidget {
  const InfoPg2({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('pg2')));
  }
}
